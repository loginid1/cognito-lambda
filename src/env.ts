const env = {
  LOGINID_BASE_URL: process.env.LOGINID_BASE_URL || "",
  LOGINID_CLIENT_ID: process.env.LOGINID_CLIENT_ID || "",
  COGNITO_USER_POOL_ID: process.env.COGNITO_USER_POOL_ID || "",
  COGNITO_CLIENT_ID: process.env.COGNITO_CLIENT_ID || "",
};

export default env;
