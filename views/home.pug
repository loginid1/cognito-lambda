extends template.pug

block scripts
	script(src="../src/home.ts" type="module")

block content
	section
		div(class="container h-100")
			div(class="row justify-content-sm-center h-100")
				div(class="col-xxl-4 col-xl-5 col-lg-5 col-md-7 col-sm-9")
					div(class="card shadow-lg")
						div(class="card-body p-5")
							h1.mb-5 Hello  
							form(class="needs-validation")
								div(class="align-items-center d-flex mb-2")
									button(type="submit" class="btn btn-primary w-100 ms-auto" id="passwordless-btn" data-type="ADD_FIDO2") Add Passwordless
								div(class="align-items-center d-flex")
									button(type="submit" class="btn btn-primary w-100 ms-auto" id="logout-btn" data-type="HOME_LOGOUT") Logout
